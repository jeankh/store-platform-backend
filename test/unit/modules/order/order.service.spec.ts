import { describe, it } from "vitest";

describe("Order unit tests", () => {
  it.todo("creates order from checkout snapshot");
  it.todo("persists item and address snapshots correctly");
  it.todo("rejects order creation from missing checkout");
  it.todo("records status transition history");
  it.todo("adds order note");
});
