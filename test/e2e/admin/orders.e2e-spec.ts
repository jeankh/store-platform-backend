import { describe, it } from "vitest";

describe("Admin orders e2e tests", () => {
  it.todo("GET /api/admin/orders lists orders");
  it.todo("GET /api/admin/orders/:orderId returns order");
  it.todo("PATCH /api/admin/orders/:orderId/status updates status");
  it.todo("POST /api/admin/orders/:orderId/notes creates order note");
});
